# v2 — 2026-04-16 メール認証/Sentry/OCR追加
fastapi==0.135.2
uvicorn[standard]==0.42.0
python-dotenv==1.2.2
pydantic==2.12.5
python-multipart==0.0.22

# 認証
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
cryptography==46.0.6
google-auth>=2.0.0  # Flutter Google Sign-In ID トークン検証用

# サーバー
gunicorn>=21.0.0

# 監視
sentry-sdk[fastapi]>=2.0.0

# セキュリティ
slowapi==0.1.9

# 決済
stripe>=11.0.0,<12.0.0

# メール送信
resend>=2.0.0

# HTTP クライアント
httpx==0.28.1

# AI（複数プロバイダ対応 — どれか1つでも構成されていれば動作）
anthropic==0.86.0
openai>=1.40.0
google-generativeai>=0.8.0
google-genai>=1.0.0

# ao_research 依存
ddgs
pytrends
rich
pdfplumber

# OCR (スキャンPDF対応)
pytesseract>=0.3.10
Pillow>=10.0.0
